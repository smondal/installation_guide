sudo apt-get update
sudo aptitude install build-essential libssl-dev libreadline5 libreadline5-dev zlib1g zlib1g-dev
sudo apt-get install curl  -y
sudo atp-get install git-core  -y
user$ bash < <(curl -s https://rvm.beginrescueend.com/install/rvm)

bash < <(curl -s https://rvm.beginrescueend.com/install/rvm)
rvm install 1.9.2
rvm install 1.8.7
rvm install bundler
rvm 1.9.2 install rails
sudo apt-get install mysql-server -y (password:)
sudo apt-get install phpmyadmin -y (password:)
sudo ln -s /usr/share/phpmyadmin /var/www/phpmyadmin
sudo apt-get install sun-java6-jdk
sudo apt-get install libmysql-ruby libmysqlclient-dev libmysqlclient12-dev

Q.Netbeen Install from the following site
 http://dlc.sun.com.edgesuite.net/netbeans/6.9/rc/
 Emacs install from package
 Synacptic package manager

Q. Firefox installation
   sudo apt-get-repository ppa:mozillateam/firefox-stable
   sudo apt-get update
   sudo apt-get install firefox
q. how
